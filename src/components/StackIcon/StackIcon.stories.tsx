import React from "react";
import StackIcon, { StackIconProps } from "./StackIcon";

export default {
  title: "StackIcon",
  component: StackIcon,
};

// @ts-ignore
export const Default = (props: StackiconProps) => <StackIcon {...props} />;
